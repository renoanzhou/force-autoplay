import { canAutoplay as check } from './can-autoplay';
import { forceAutoplay as force } from './force-autoplay';
declare const _default: {
    check: typeof check;
    force: typeof force;
};
export default _default;
export * from './types';
